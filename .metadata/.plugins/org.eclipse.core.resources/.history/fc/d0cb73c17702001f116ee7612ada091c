package course;

import java.util.Locale;
import java.util.Scanner;

import entities.Entities;

public class Ex05 {

	public static void main(String[] args) {
		Locale.setDefault(Locale.US);
		Scanner sc = new Scanner(System.in);

		System.out.print("Entre com o Numero da conta: ");
		int conta = sc.nextInt();

		System.out.printf("Entre com o nome do Usuario: ");
		String usuario = sc.next();

		System.out.printf("Haverá um deposito inicial? (y/n) ");
		String resp = sc.next();
		
		double saldo = 0;
		if (resp.equals("y")) {
			System.out.printf("Entre com o valor inicial do deposito: ");
			saldo = sc.nextDouble();
		} else {
			System.out.println("Sem deposito inicial!");
		} 
		
		Entities entities = new Entities(conta, usuario, resp, saldo);

		System.out.println("Dados da Conta: ");
//		System.out.printf("Conta %d, Usuario: %s, Saldo: $ %.2f", entities.getConta(), entities.getUsuario(), entities.getSaldo());
		System.out.println(entities.getConta() + entities.getSaldo() + entities.getUsuario());

	}

}
